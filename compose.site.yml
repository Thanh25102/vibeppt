services:
  site:
    image: ghcr.io/thanh25102/vibeppt-site:${VIBEPPT_SITE_TAG:-latest}
    restart: unless-stopped
    ports:
      - "${VIBEPPT_SITE_PORT:-8080}:8080"
    read_only: true
    tmpfs:
      - /tmp:size=16m
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
